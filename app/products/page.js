import ProductList from '@/components/ProductList';

export const metadata = {
    title: 'Products - MovieNest',
    description: 'View all categories and products',
};

export default function ProductsPage() {
    return <ProductList />;
}