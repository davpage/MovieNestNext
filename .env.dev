PROTOCOL="http"
HOSTNAME="localhost:4000"
BACKEND_DOMAIN="${PROTOCOL}://${HOSTNAME}/"
API_IMAGE_PATH_ENDPOINT="storage/"
FRONT_URL="${PROTOCOL}://${HOSTNAME}/"
NEXT_PUBLIC_API_ENDPOINT=http://localhost:4000
NEXT_PUBLIC_IMAGE_API_ENDPOINT=http://localhost:4000/storage
